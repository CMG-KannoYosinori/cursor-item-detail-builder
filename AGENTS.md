# AGENTS.md

このファイルは、Cursor Agent がこのリポジトリで作業する際のガイドです。

## このリポジトリについて

これはモラタメ（moratame.net）の案件向けに商品詳細ページの HTML を作るための**テンプレートリポジトリ**です。アプリケーションではないため、ビルドツール・パッケージマネージャ・テストは一切ありません。実際の案件では `00000html/` テンプレートをコピーし、プレースホルダ `00000` を案件のプロジェクト ID（数字）に置換したうえで、その案件用に HTML/CSS を編集します（`00000html/` 自体は改変せず、常にテンプレートとして残す）。

`00000html/` はテンプレート、`{id}html/`（例: `15066html/`）はそこから作った案件ごとのディレクトリです。両方とも Git 管理下に置かれます。

```
00000html/
├── はじめてガイド.md   # 未経験者向けの最短手順（作業フローはまずこちら）
├── README.md            # くわしいリファレンス（BEM 規約・クラス一覧・開発環境）
├── dist/                 # 納品物一式（HTML・CSS・JS・画像）。ここを直接編集する
└── .vscode/              # Live Server の設定
```

`はじめてガイド.md` と `README.md` は、案件ごとの作業フロー（プレースホルダ置換・BEM 規約・公開前チェックリスト）についての正式なドキュメントです。構造に関わる変更をする前に必ず目を通してください。このファイルはすぐに作業を始められるように要点だけをまとめたものです。

## ツールチェーン — npm も CLI ビルドもなし

開発はターミナルではなく、VS Code / Cursor 上で **Live Server** 拡張機能だけで完結します。`dist/*.html` を Live Server で開いてプレビューします。ビルド・監視ステップは一切ありません。

`dist/styles/style.css` は生成物ではなく、**このファイル自体が編集対象**です（Sass などのプリプロセッサは使わない）。保存すれば Live Server が自動でブラウザをリロードします。

このリポジトリに lint / 型チェック / テストのコマンドは存在しません。新たに作ろうとしないでください。変更の確認は Live Server でのブラウザ表示確認のみです。

## くわしいルール（別ファイル）

トピックごとに分けています。該当する作業をする前に読んでください。

- `docs/css-architecture.md` — CSS の構成（Modern BEM・コメントセクション区切り）と守るべきルール
- `docs/project-workflow.md` — `00000` → プロジェクト ID の置換手順、ローカル／CDN アセットの切り替え、編集用／参照用の 2 ファイル構成
- `docs/coding-workflow.md` — `/coding` による、デザインカンプからの商品概要コーディング対話フロー
- `docs/commit-convention.md` — コミット時の gitmoji・粒度ルール

Cursor 向けの分割ルールは `.cursor/rules/`、`/coding` の定義は `.cursor/skills/coding/SKILL.md` にある。

## 商品概要のコーディング

デザインカンプ・画像アセットをもとに商品概要エリアをコーディングする作業は、必ず `/coding` から始める。会話中の自然文だけで推測して着手しない。チャット内で「コーディングして」等と自然文で話しかけられた場合は `/coding` の利用を案内する。詳細は `docs/coding-workflow.md` を参照。
